# Agnes Docs Index (session 2026-08-26)
Doc index: https://wiki.agnes-ai.com/llms.txt
Video model: `agnes-video-2.5-flash`, endpoint `POST /v1/videos`, retrieve `GET /agnesapi?video_id=...&model_name=...`, size fixed `720P`, async queued→pending→in_progress→completed.
Image queue can be full (`text image queue is full`) — retry later.
Base URL: https://apihub.agnes-ai.com/v1; verify key with `/v1/models` first.
